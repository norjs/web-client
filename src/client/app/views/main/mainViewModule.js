import angular from 'angular';
import mainViewComponent from './mainViewComponent';

export default angular.module(
  "norjs.app.views.main"
  , [

  ])
  .component('nrMainView', mainViewComponent)
  .name;
